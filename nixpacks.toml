[providers]
node = "18"

[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = ['npm ci --prefer-offline --no-audit']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node server.js'

[variables]
NODE_ENV = 'production'
NPM_CONFIG_PRODUCTION = 'false'

